import React from 'react'
import { container, title, slogan, logo } from './styles.css'

const Home = props => (
  <div className={container}>
    <p className={title}>Hai-There</p>
    <p className={slogan}>React // Redux // Firebase</p>
  </div>
)

export default Home
